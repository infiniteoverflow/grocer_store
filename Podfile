# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'grocer_store' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for grocer_store
  pod 'lottie-ios'

  target 'grocer_storeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'grocer_storeUITests' do
    # Pods for testing
  end

end
